[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "sbeam"
version = "0.2.0"
description = "Simple beam finite element analysis (SOL 101 / SOL 103) with NASTRAN BDF input."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {name = "Sean O'Meara", email = "sean.c.omeara74@gmail.com"},
]
keywords = ["fea", "beam", "nastran", "structural-analysis", "modal-analysis"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "numpy",
    "scipy",
    "pandas",
]

[project.optional-dependencies]
# The GUI's dependencies are an extra, not a base requirement: a solver run in
# CI or a batch pipeline has no use for a web framework.  Enforced by
# tests/tools/test_import_boundary.py, which fails if sbeam/ imports either.
viewer = [
    "plotly",
    "streamlit",
]
dev = [
    "pytest",
    "pytest-cov",
    "ruff",
    "pyright",
]

[project.scripts]
sbeam = "sbeam.main:main"
sbeam-cases = "sbeam_tools.cases.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["sbeam*", "sbeam_tools*"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = [
    "E702",  # semicolons in matrix assignments — intentional paired style
    "E501",  # NASTRAN fixed-width format strings make a hard line limit impractical
    "E741",  # I, A, L, E are standard FEA/structural engineering variable names
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "--cov=sbeam --cov-report=term-missing"

[tool.coverage.run]
branch = true
source = ["sbeam"]

[tool.coverage.report]
show_missing = true
skip_empty = true
